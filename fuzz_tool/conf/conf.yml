common:

  mlir_build_path: '../MLIRFuzzer'   # mlirfuzzer-opt build path
  mlir_opt: '/build/bin/mlirfuzzer-opt'  # the tool used to Tosa graph generation and IR mutation
  temp_dir: '../logs/' # Used to save intermediates 


# set database info
database:
  host: "10.15.1.202"
  port: 23515
  username: "root"
  passwd: "root123"
  db: "mlir"

# set tosa graph generator
generator:
  empty_func_file: "./case/empty.mlir"  # empty fuction, used to generate tosa graph
  count: 10  # the number of tosa graphs

fuzz:
  analysis_seed_file: "/src/temp.json" 
  run_time: 2000
  Nmax: 1
  mutate_flag: "mutate"
  flag_mutate: 1 
  flag_VPA: 0
  diff_threshold: 0.1
